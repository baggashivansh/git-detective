package com.gitdetective.entity;

public enum InvestigationRelationshipType {
    IMPORTS,
    IMPLEMENTS,
    EXTENDS,
    CALLS,
    USES,
    OWNS,
    REFERENCES,
    BELONGS_TO,
    COMMITS,
    MODIFIED_BY
}
