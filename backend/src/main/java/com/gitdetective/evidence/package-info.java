/**
 * Evidence Engine bounded context.
 *
 * <p>Aggregates deterministic investigation outputs into immutable {@code EvidenceBundle} instances
 * for future AI consumption. Contains no AI, LLM, or prompt logic.
 */
package com.gitdetective.evidence;
