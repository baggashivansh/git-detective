/**
 * Intentionally has no public HTTP endpoints. The Evidence Engine is an internal bounded context.
 * The Phase 4 Assistant consumes {@code EvidenceEngine} via dependency injection only.
 */
package com.gitdetective.evidence.controller;
